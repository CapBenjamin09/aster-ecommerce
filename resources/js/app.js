// Initialization for ES Users
import {
    Collapse,
    Dropdown,
    initTE,
} from "tw-elements";

initTE({ Collapse, Dropdown });

// Initialization for ES Users
import {
    Carousel,
} from "tw-elements";

initTE({ Carousel });
